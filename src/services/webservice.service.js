async function getSolicitud(numeroSolicitud) {
  // TODO: conectar con el Web Service real cuando se definan URL y contrato.
  throw new Error(
    `Consulta no disponible para la solicitud ${numeroSolicitud}: Web Service pendiente de configurar.`,
  );
}

module.exports = {
  getSolicitud,
};
